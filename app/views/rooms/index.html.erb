<div class="row">
  <div class="col-12 col-md-3">
    <!-- <div class="mb-3">
      <%= link_to new_club_room_path(@club), class: "btn btn-primary" do %>
        Create a room
      <% end %>
    </div> -->
    <div>
      Your Chat Rooms
    </div>

    <% if @rooms.present? %>
      <nav class="nav flex-column">
        <% @rooms.each do |room| %>
          <%= link_to room.name, club_room_path(@club, room), class: "nav-link room-nav-link" %>
        <% end %>
      </nav>
    <% else %>
      <div class="text-muted">
        The are no rooms
      </div>
    <% end %>
  </div>
</div>

  <!-- <div class="col">
    <div class="alert alert-primary">
      <h4 class="alert-heading">
        Welcome to the Chat!
      </h4>

      <hr />

      <p>
        You can create or join a room from the sidebar.
      </p>
    </div>
</div> -->
